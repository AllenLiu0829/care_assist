import 'package:flutter/material.dart';

class ProductPageViewModel extends StatelessWidget {
  const ProductPageViewModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
